# Arcont Poly Haven forest authoring pack

Selected source assets are CC0. The live API is used only by the ingest tool; the game has no runtime network dependency. Powered by Poly Haven.

The committed derivatives are intentionally bounded for mobile evaluation. See `MANIFEST.json` for source URLs, authorship metadata and SHA-256 hashes.
